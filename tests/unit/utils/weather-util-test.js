import weatherUtil from 'mail-app/utils/weather-util';
import { module, test } from 'qunit';

module('Unit | Utility | weather util');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = weatherUtil();
  assert.ok(result);
});
